

<div class="row">
<center>
	<div class="col-md-6">
		<h3 class="burgers">Burgers To Devour</h3>
			<div id="readyEat">
			{{things}}
			{{!-- 	{{#each burgers}}
					{{#unless devoured}}
					<form action="burger/eat/{{id}}?_method=PUT" method="post">
						<div class="input-group burgers">
							<li class="form-control">{{id}}. {{burger_name}}</li>
								<span class="input-group-btn">
									<button class="btn btn-success devour-button" type="submit">Devour!</button>
								</span>
							</div>
						</form>			
					{{/unless}}
				{{/each}} --}}
		</div>
				
				
	</div>
</center>

	{{!-- Devoured Column  --}}
	<center>
		<div class="col-md-6">
			<h3 class="devoured_burgers">Devoured Burgers</h3>
				<div id="devoured">				
				{{!-- 	{{#each burgers}}
						{{#if devoured}}
							<li class="form-control">{{id}}. {{burger_name}}</li>
						{{/if}}
					{{/each}} --}}
				</div>
			</div>
		</center>					

</div>					
<br><br>

<div class="row">
	<center>
	<form class="input-group burger" action="create" method="post">
		<input type="text" name="mealName"></input>
			<button type="submit">Create A Creation</button>
	</form>	
	</center>
</div>	


</div>